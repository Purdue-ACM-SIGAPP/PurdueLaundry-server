describe('Controllers', () => {
	describe('MachineController', () => {
		describe('getMachines', () => {
			it('gets more than one location', () => {

			});
		});

		describe('getMachinesAtLocation', () => {
			it('only has one location', () => {

			});
		});

		describe('getPossibleStatuses', () => {
			it('is correct', () => {

			});
		});
	});
});