beforeEach(function() {
	// There is currently no need for a RedisHelper, but I will keep
	// this file here for structure's sake. If you create a test
	// that needs a custom matcher, you are encouraged to delete
	// this comment and make a custom matcher
});